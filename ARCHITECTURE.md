# 🏗️ Twitter Follower Cleanup - Architecture & Flow

## 📁 File Structure

```
x_bots_purge/
│
├── main.py                 # 🚀 Entry Point - CLI & orchestration
│   └── Imports from:
│       ├── utils.py
│       └── twitter_cleaner.py
│
├── twitter_cleaner.py      # 🤖 Core Logic - Browser automation
│   └── Imports from:
│       ├── config.py
│       └── utils.py
│
├── utils.py                # 🔧 Utilities - Bot detection, logging, reports
│   └── Imports from:
│       └── config.py
│
├── config.py               # ⚙️ Configuration - All settings & selectors
│   └── No imports (base module)
│
└── [Generated Directories]
    ├── browser_data/       # 🌐 Persistent browser session
    ├── reports/            # 📊 JSON/CSV reports
    ├── backups/            # 💾 Removed followers backup
    └── screenshots/        # 📸 Debug screenshots
```

## 🔄 Execution Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              USER RUNS COMMAND                               │
│   python main.py --user-id realsamzer --limit 50 [--dry-run] [--from-end]  │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           main.py: main()                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  1. parse_arguments()         → Parse CLI args                              │
│  2. setup_logging()           → Configure logging (from utils.py)           │
│  3. print_banner()            → Display welcome banner                      │
│  4. asyncio.run(main_async()) → Start async execution                       │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                       main.py: main_async()                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│  async with TwitterCleaner(...) as cleaner:                                 │
│      └── Calls cleaner.run(dry_run, limit, skip_confirmation, from_end)    │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│              twitter_cleaner.py: TwitterCleaner.__aenter__()                │
├─────────────────────────────────────────────────────────────────────────────┤
│  initialize_browser()                                                       │
│  ├── Launch Playwright                                                      │
│  ├── Create persistent context (saves login)                                │
│  ├── Set fullscreen/maximized mode                                          │
│  └── Get or create page                                                     │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                   twitter_cleaner.py: run()                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ STEP 1: wait_for_login()                                            │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │  • Navigate to x.com                                                │   │
│  │  • Check if already logged in (_is_logged_in)                       │   │
│  │  • If not: prompt user to login manually                            │   │
│  │  • Poll every 2s until login detected (max 5 min)                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ STEP 2: navigate_to_followers()                                     │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │  • Navigate to x.com/{user_id}/followers                            │   │
│  │  • Handle page errors (_handle_page_error)                          │   │
│  │  • Wait for follower cells to appear                                │   │
│  │  • Retry up to 3 times if failed                                    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ STEP 3: scan_and_remove_in_batches()                                │   │
│  │         (THE MAIN LOOP - see detailed flow below)                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ STEP 4: Generate Reports                                            │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │  • save_report() → JSON + CSV                                       │   │
│  │  • save_backup() → Removed followers list                           │   │
│  │  • print_summary() → Console output                                 │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│              twitter_cleaner.py: TwitterCleaner.__aexit__()                 │
├─────────────────────────────────────────────────────────────────────────────┤
│  cleanup()                                                                  │
│  ├── Close browser context                                                  │
│  └── Stop Playwright                                                        │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🔁 Detailed: scan_and_remove_in_batches() Loop

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    scan_and_remove_in_batches()                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                    ┌─────────────────┴─────────────────┐
                    │ if from_end:                      │
                    │   _scroll_to_end_of_list()        │
                    │   (scroll down until no more)     │
                    └─────────────────┬─────────────────┘
                                      │
                                      ▼
          ┌──────────────────────────────────────────────────┐
          │                                                  │
          │     ╔════════════════════════════════════════╗   │
          │     ║     MAIN LOOP (max 50 iterations)      ║   │
          │     ╚════════════════════════════════════════╝   │
          │                        │                         │
          │                        ▼                         │
          │     ┌────────────────────────────────────────┐   │
          │     │ 1. _handle_page_error()                │   │
          │     │    └─ Click "Retry" if error shown     │   │
          │     └────────────────────────────────────────┘   │
          │                        │                         │
          │                        ▼                         │
          │     ┌────────────────────────────────────────┐   │
          │     │ 2. Get visible cells                   │   │
          │     │    page.query_selector_all(UserCell)   │   │
          │     └────────────────────────────────────────┘   │
          │                        │                         │
          │                        ▼                         │
          │     ┌────────────────────────────────────────┐   │
          │     │ 3. For each cell:                      │   │
          │     │    ├─ _extract_follower_info(cell)     │   │
          │     │    │   └─ Get username from href       │   │
          │     │    │                                   │   │
          │     │    ├─ is_bot_username(username)        │   │ ◄── utils.py
          │     │    │   └─ Check regex patterns         │   │
          │     │    │                                   │   │
          │     │    └─ If bot & not dry_run:            │   │
          │     │        └─ Add to batch_bots list       │   │
          │     └────────────────────────────────────────┘   │
          │                        │                         │
          │                        ▼                         │
          │     ┌────────────────────────────────────────┐   │
          │     │ 4. Process batch_bots:                 │   │
          │     │    ├─ First batch? Ask confirmation    │   │
          │     │    │                                   │   │
          │     │    └─ For each bot:                    │   │
          │     │        _remove_follower_from_cell()    │   │
          │     │        ├─ scroll_into_view()           │   │
          │     │        ├─ Click menu (⋯) button        │   │
          │     │        ├─ _find_remove_button()        │   │
          │     │        ├─ Click "Remove follower"      │   │
          │     │        └─ _handle_confirmation_dialog()│   │
          │     └────────────────────────────────────────┘   │
          │                        │                         │
          │                        ▼                         │
          │     ┌────────────────────────────────────────┐   │
          │     │ 5. Check exit conditions:              │   │
          │     │    ├─ removal_limit reached?           │   │
          │     │    ├─ max_scroll_attempts reached?     │   │
          │     │    └─ no_new_followers 3x in a row?    │   │
          │     └────────────────────────────────────────┘   │
          │                        │                         │
          │               ┌───────┴───────┐                  │
          │               │ Exit?         │                  │
          │               └───────┬───────┘                  │
          │            NO ◄───────┴───────► YES              │
          │             │                    │               │
          │             ▼                    │               │
          │     ┌───────────────────┐        │               │
          │     │ 6. Scroll page    │        │               │
          │     │  └─ scrollBy(600) │        │               │
          │     │    (or -600 if    │        │               │
          │     │     from_end)     │        │               │
          │     └─────────┬─────────┘        │               │
          │               │                  │               │
          │               └────► LOOP ◄──────┘               │
          │                                                  │
          └──────────────────────────────────────────────────┘
```

## 🔍 Bot Detection Flow (utils.py)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    is_bot_username(username)                                │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
                    ┌─────────────────────────────────┐
                    │ Check primary pattern:          │
                    │ digit_suffix_pattern            │
                    │ (.*\d{5,}$)                     │
                    │                                 │
                    │ "user12345678" → MATCH (bot)    │
                    │ "user123" → NO MATCH            │
                    └─────────────────┬───────────────┘
                                      │
                         ┌────────────┴────────────┐
                         │                         │
                      MATCH                    NO MATCH
                         │                         │
                         ▼                         ▼
              ┌──────────────────┐    ┌───────────────────────┐
              │ Return:          │    │ Check suspicious      │
              │ (True,           │    │ patterns array:       │
              │  "ends with 5+   │    │                       │
              │   digits")       │    │ • ^[a-z]+\d{8}$       │
              └──────────────────┘    │ • ^\w+_\d{5,}$        │
                                      │ • \d{6,}$             │
                                      │ • ... etc             │
                                      └───────────┬───────────┘
                                                  │
                                     ┌────────────┴────────────┐
                                     │                         │
                                  MATCH                    NO MATCH
                                     │                         │
                                     ▼                         ▼
                          ┌──────────────────┐    ┌──────────────────┐
                          │ Return:          │    │ Return:          │
                          │ (True,           │    │ (False, "")      │
                          │  "Matches        │    │ → Not a bot      │
                          │   pattern: ...")  │    └──────────────────┘
                          └──────────────────┘
```

## 📊 Class Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            TwitterCleaner                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│ Attributes:                                                                 │
│   - user_id: str                                                            │
│   - playwright: Playwright                                                  │
│   - context: BrowserContext                                                 │
│   - page: Page                                                              │
│   - followers: List[FollowerInfo]                                           │
│   - scanned_usernames: Set[str]                                             │
│   - removed_count: int                                                      │
│   - failed_count: int                                                       │
│   - report: CleanupReport                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│ Methods:                                                                    │
│   + __aenter__() → self                                                     │
│   + __aexit__()                                                             │
│   + initialize_browser()                                                    │
│   + cleanup()                                                               │
│   + wait_for_login() → bool                                                 │
│   + navigate_to_followers() → bool                                          │
│   + scan_and_remove_in_batches(dry_run, limit, confirm, from_end) → int    │
│   + run(dry_run, limit, skip_confirmation, from_end) → CleanupReport       │
│   - _is_logged_in() → bool                                                  │
│   - _handle_page_error() → bool                                             │
│   - _scroll_to_end_of_list()                                                │
│   - _extract_follower_info(cell) → FollowerInfo                            │
│   - _find_user_cell(username) → Element                                     │
│   - _remove_follower_from_cell(cell, username) → bool                       │
│   - _find_remove_button() → Element                                         │
│   - _handle_confirmation_dialog()                                           │
│   + take_screenshot(name) → str                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ uses
                                      ▼
┌─────────────────────────────┐    ┌─────────────────────────────┐
│     FollowerInfo            │    │     CleanupReport           │
├─────────────────────────────┤    ├─────────────────────────────┤
│ - username: str             │    │ - session_start: str        │
│ - display_name: str         │    │ - session_end: str          │
│ - is_bot: bool              │    │ - user_id: str              │
│ - bot_reason: str           │    │ - total_followers_scanned   │
│ - removed: bool             │    │ - bot_accounts_identified   │
│ - removal_error: str        │    │ - successfully_removed      │
│ - timestamp: str            │    │ - failed_removals           │
└─────────────────────────────┘    │ - dry_run: bool             │
                                   │ - followers: List[Dict]     │
                                   │ - errors: List[str]         │
                                   └─────────────────────────────┘
```

## ⚙️ Configuration (config.py)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              CONFIG MODULES                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  BROWSER_CONFIG          DELAYS                  LIMITS                     │
│  ┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐       │
│  │ headless: False │     │ between_removals│     │ max_removals:   │       │
│  │ slow_mo: 100    │     │   : 2.0s        │     │   1000          │       │
│  │ viewport: None  │     │ after_scroll:   │     │ max_retry: 3    │       │
│  │ start_maximized │     │   1.5s          │     │ max_scroll: 50  │ ◄────┐│
│  │   : True        │     │ menu_animation: │     │ batch_size: 10  │      ││
│  │ user_data_dir:  │     │   0.5s          │     └─────────────────┘      ││
│  │   ./browser_data│     │ page_load: 3.0s │                              ││
│  └─────────────────┘     └─────────────────┘     ⚠️ Increase this to      ││
│                                                    scan more followers!────┘│
│                                                                             │
│  SELECTORS               BOT_DETECTION                                      │
│  ┌─────────────────┐     ┌──────────────────────────────────────────┐      │
│  │ follower_cell:  │     │ digit_suffix_pattern: r".*\d{5,}$"       │      │
│  │   UserCell      │     │                                          │      │
│  │ user_name_link: │     │ suspicious_patterns: [                   │      │
│  │   a[href^="/"]  │     │   r"^[a-z]+\d{8}$",                      │      │
│  │ more_menu_btn:  │     │   r"^\w+_\d{5,}$",                       │      │
│  │   [data-testid= │     │   r"\d{6,}$",                            │      │
│  │    "caret"]     │     │   ... and more                           │      │
│  │ menu_item:      │     │ ]                                        │      │
│  │   [role=        │     └──────────────────────────────────────────┘      │
│  │    "menuitem"]  │                                                       │
│  └─────────────────┘                                                       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🎯 Quick Reference: CLI Options Flow

```
python main.py [OPTIONS]

--user-id, -u     ──────────────────► TwitterCleaner.user_id
                                      └─► URL: x.com/{user_id}/followers

--dry-run, -n     ──────────────────► scan_and_remove_in_batches(dry_run=True)
                                      └─► Skip removal, only identify

--limit, -l       ──────────────────► scan_and_remove_in_batches(limit=N)
                                      └─► Stop after N removals

--from-end        ──────────────────► scan_and_remove_in_batches(from_end=True)
                                      └─► Scroll to bottom first, then scan upward

--yes, -y         ──────────────────► run(skip_confirmation=True)
                                      └─► No prompts, auto-confirm

--verbose, -v     ──────────────────► setup_logging(verbose=True)
                                      └─► DEBUG level logs
```

